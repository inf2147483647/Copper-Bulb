# 更新日志

本项目所有重要版本变更都会记录在此文件中。

## [2.0.0.0] - 2026-09-08

首个正式版本。铜灯是受 Minecraft 铜灯启发的 ClassIsland 自动化**条件**插件，它像一个由红石控制的 T 触发器（Toggle Flip-flop），把"信号脉冲"变成状态的翻转与保持。

### 新增

- 新增 **铜灯** 自动化条件，可在【应用设置】→【自动化】→ 规则集的条件列表中使用。
- 支持嵌套：每个铜灯内部可以添加任意其他自动化条件，铜灯里还能放铜灯。
- 边沿触发翻转：当内部条件**从不满足变为满足**（相当于收到一个红石脉冲）时，铜灯在"点亮"与"熄灭"之间翻转一次。
- 下降沿保持：当内部条件**从满足变回不满足**时，铜灯不翻转，保持当前状态。
- 状态持久化：点亮/熄灭状态会保存到配置文件，重启 ClassIsland 后依然保持。
- 提供内嵌规则集设置界面，可在铜灯设置中添加内部条件。

### 兼容性

- 基于 ClassIsland 插件 SDK `1.7.106.2-dev-v2` 构建，面向 .NET 8（`net8.0-windows`）。
- 与 ExtraIsland 等其他插件兼容：铜灯服务采用懒解析方式获取自动化服务，插件加载顺序不会互相影响。

### 说明

- 内部条件状态每秒检测一次，翻转发生在状态变化的下一秒。
- 使用示例：内部条件放"上课中"。第一节课开始时铜灯点亮；下课（条件不满足）不翻转；第二节课开始（再次满足，产生上升沿）铜灯熄灭。如此交替，可用于单双周轮换、单双日切换等场景。

### 安装

将 `Copper_Bulb.cipx` 插件包放入 ClassIsland 的插件目录，或在 ClassIsland 中通过插件管理导入即可。
